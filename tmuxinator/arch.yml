# ~/.tmuxinator/arch.yml

name: arch
root: ~/

windows:
  - console:
      layout: main-horizontal
      panes:
        - # open bash
        - # open bash
  - tmp:
      layout: main-horizontal
      panes:
        - cd ~/tmp
        - cd ~/tmp
  - git:
      layout: main-horizontal
      panes:
        - cd ~/git
        - cd ~/git
